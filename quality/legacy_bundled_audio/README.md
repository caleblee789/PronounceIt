# Historical bundled audio

These byte-preserved clips, subset checklist, provenance, and Azure review ledger document earlier releases. They are not runtime inputs and are excluded from the add-on archive. The current application uses one complete, separately downloaded audio library.
